include modules/head
body
  .row.center-align
    each item in value
      .col.center-align.s12
        .card.blue-grey.darken-1
          .card-content.white-text
            span.card-title До вас доебалась: <span class="amber-text">#{item.name}</span>
            span.card-title.card-p По причине: <span class="amber-text">#{item.reason}</span>
            span.card-title.card-p Куда ехать: <span class="amber-text">#{item.contacts}</span>
            span.card-title.card-p Когда случилось: <span class="amber-text">#{item.date}</span>
            p.card-p #{item.content}
  include modules/scripts